cd C:\Users\gkazarkin\gitfiles\files
git status
git pull
git add *
git commit -m "text"
git push

cd путь
git status
(локально)
ls
(показывает файлы)
git add *
git commit -m
git commit -a -m
git commit -am
(все файлы добавл€ет в репозиторий)

git fetch
(есть ли измеени€ удалЄнно)
git pull
(ст€гивает файлы с удалЄнки)
git push
(заливает файлы в репозиторий)

git diff
(показывает разницу)
git show коммит
(показывает изменени€ в коммите)

git merge --abort
(отмена коммита)

git checkout название‘айла
(возвращает файл в состо€ние до изменени€)

git stash
(отмен€т изменени€ временно)
git stash pop
(возвращает)
git stash clear
(удал€ет из стэша)